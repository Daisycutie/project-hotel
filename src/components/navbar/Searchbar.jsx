import { Input } from "@/components/ui/input"

const Searchbar = () => {
  return (
    <Input 
    type="text"
    placeholder="Search..."
    className="max-w-xl"
    />
  )
}

export default Searchbar
